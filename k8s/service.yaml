apiVersion: v1
kind: Service
metadata:
  name: ota-asset
  labels:
    app: ota-asset
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: ota-asset

